# dependencies
/node_modules

# next.js
/.next
/out

# local database (PGlite, file-backed)
/.pglite

# typescript
*.tsbuildinfo
next-env.d.ts

# env — never commit files that can hold API keys
.env
.env.local
.env*.local
.env.development
.env.production

# misc
.DS_Store

# evalite (only if you opt into persistent SQLite storage; default is in-memory)
/evalite.db
/.evalite

# private reference solution — must NOT ship in the public/candidate repo.
# Move this into your private repo; it's here only so the full build isn't lost.
/_reference
